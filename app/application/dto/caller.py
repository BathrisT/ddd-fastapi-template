"""Кто пришёл — результат резолюции, а не то, что предъявили на входе.

Отделено от способа проверки намеренно. Предъявляют разное и по-разному:
HTTP — заголовок, очередь — поле в `message.kwargs`, следующий протокол
придумает своё. А вот «кто это оказался» одинаково для всех входов, и дальше по
слоям едет именно оно.

В шаблоне звонящий один на приложение, поэтому здесь только его имя. Место, где
появится настоящий идентификатор — арендатор, пользователь, внешняя система, —
именно это поле: тогда `Caller` начнёт нести `tenant_id`/`user_id`, а всё, что
ниже, продолжит читать его так же, как читало имя.
"""

from dataclasses import dataclass


@dataclass(frozen=True)
class Caller:
    name: str
