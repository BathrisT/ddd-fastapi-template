"""Порт обращения к языковой модели.

**Метод называет намерение, а не транспорт.** `complete(prompt)` портом не
является: промпт — это деталь реализации, и, уехав в сценарий, он тащит туда же
формат ответа, язык, версию модели и её капризы. Сценарий обязан спрашивать
«дай приветствие для такого-то», а как это спросить у модели — знает адаптер, и
он один.

Отсюда же и практическое следствие: дублёр порта в тесте пишется в три строки и
не притворяется моделью, а подмена промпта не ломает ни один тест сценария —
потому что сценарий про промпт не знает.

Ключ, адрес и таймаут в порт тем более не входят: это настройки транспорта, они
живут в реализации и приходят ей при сборке.
"""

from typing import Protocol


class AiService(Protocol):
    async def welcome_text(self, name: str) -> str:
        """Приветствие новому пользователю одной фразой.

        Отказ транспорта поднимается наружу: решать, что делать с недоступной
        моделью, — дело вызывающего, а не адаптера.
        """
        ...
