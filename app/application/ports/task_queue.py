"""Порт постановки задачи в очередь — по имени обработчика.

Имя, а не функция: порт живёт в application, а обработчики — во входном слое,
и тип отсюда на них ссылаться не может. Вызывающий пишет `handler.__name__` —
ссылка на сам объект, поэтому опечатку ловит импорт, а не рантайм.

Сослаться на незарегистрированный обработчик всё равно нельзя: `enqueue` не
находит такую задачу у брокера и падает на месте постановки, а не молча.
"""

from typing import Protocol


class TaskQueue(Protocol):
    async def enqueue(self, task_name: str, **kwargs: object) -> str:
        """Ставит задачу и возвращает её идентификатор — по нему опрашивают результат."""
        ...
