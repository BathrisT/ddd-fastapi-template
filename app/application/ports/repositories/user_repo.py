"""Порт хранилища пользователей.

Возвращает доменные модели, а не ORM-объекты (золотое правило 4): иначе
знание о колонках и о том, что объект привязан к живой сессии, расползается по
сценариям, и первый же `expire_on_commit` превращается в загадочный запрос из
середины бизнес-логики.
"""

from datetime import datetime
from typing import Protocol

from app.domain.models.user import User


class UserRepo(Protocol):
    async def save(self, user: User) -> User:
        """Завести нового или обновить существующего. Коммитит вызывающий.

        `id == 0` — вставка, иначе обновление (CLAUDE.md, «Идентификаторы»).
        Возвращается СОХРАНЁННЫЙ объект: у переданного идентификатор так и
        остаётся нулевым, потому что присваивает его база.

        Занятую почту поднимает `ConflictError` — уникальность стережёт база,
        и адаптер обязан перевести её отказ в доменный.

        **Обновление перезаписывает строку ЦЕЛИКОМ** снимком, который держит
        вызывающий, — не «то поле, что поменяли». Пока писатель один, это
        незаметно; появится второй, и сценарий, продержавший снимок через
        сетевой вызов, вернёт в базу устаревшие соседние поля, тихо отменив
        чужую правку. Такому сценарию мало перечитать через `get_by_id`:
        внутри одной сессии он вернёт тот же объект из identity map, не сходив
        в базу. Нужен либо явный перечитывающий метод (`populate_existing`),
        либо блокировка строки, либо версия для сравнения — то есть решение
        принимается тогда же, когда заводится второй писатель.
        """
        ...

    async def get_by_id(self, user_id: int) -> User | None: ...

    async def get_by_email(self, email: str) -> User | None: ...

    async def list_recent(self, limit: int) -> list[User]: ...

    async def delete_inactive_before(self, moment: datetime) -> int:
        """Удалить НЕАКТИВНЫХ, заведённых раньше момента. Возвращает число строк."""
        ...
