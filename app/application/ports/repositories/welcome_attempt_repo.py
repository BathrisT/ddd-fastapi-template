"""Попытки приветствия — записи, которые обязаны пережить откат сценария.

Зачем отдельно от репозитория пользователя. Обращение к языковой модели —
платное и внешнее: оно уже случилось, что бы дальше ни произошло с транзакцией
сценария. Упади сохранение результата — сценарий откатится, и вместе с ним
исчезнет след того, что мы платили. Следующий прогон обратится снова, заплатит
снова и снова может упасть.

Поэтому запись идёт своей короткой транзакцией (`AutonomousSession`), а не
сессией сценария. CLAUDE.md, «Транзакции»: для новых записей такого рода это
единственная дверь.

Порт, а не прямой вызов инфраструктуры: пишет сценарий, а он живёт в
`application` и про `AsyncSession` знать не имеет права.

**Почему `Repo` в имени, хотя по смыслу это журнал.** Соседние сторожа
опознают репозиторий по имени ТИПА В СИГНАТУРЕ — то есть по имени порта:
`check_composition` так отбивает репозиторий во входе, `check_n_plus_one` так
считает чтения в цикле. Порт, названный `WelcomeJournal`, делал слепыми обоих.
Противоречия с правилом «порт называет намерение, а не транспорт» тут нет:
`Repo` — имя паттерна, то есть намерение; транспортом было бы `Sql` или
`Postgres` в имени порта. Проверяется `scripts/check_db_access.py`.
"""

from typing import Protocol


class WelcomeAttemptRepo(Protocol):
    async def record(self, user_id: int, outcome: str) -> None:
        """Отметить попытку. `outcome`: `success` | `error`."""
        ...
