"""Попытки приветствия — записи, которые обязаны пережить откат сценария.

Обращение к модели платное и уже случилось: откатись сценарий, исчезнет и след
того, что мы платили, а следующий прогон заплатит снова. Поэтому запись идёт
своей короткой транзакцией (`AutonomousSession`) — для новых записей такого
рода это единственная дверь. Порт, а не прямой вызов: пишет сценарий, а ему
про `AsyncSession` знать нельзя.

`Repo` в имени, хотя по смыслу это журнал: соседние сторожа опознают
репозиторий по имени типа в сигнатуре, и `WelcomeJournal` ослепил бы
`check_composition` с `check_n_plus_one` разом.
"""

from typing import Protocol


class WelcomeAttemptRepo(Protocol):
    async def record(self, user_id: int, outcome: str) -> None:
        """Отметить попытку. `outcome`: `success` | `error`."""
        ...
